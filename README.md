# rifers.org website

## Running the server

~~~
./gradlew run
~~~

Go to:

[http://localhost:4242/](http://localhost:4242/)


## Deploying the app

~~~
./gradlew war
~~~

The resulting archive will be in:
`war/build/libs`