# rifers.org website

## Running the server

~~~
./gradlew run
~~~

Go to:

[http://localhost:8080/](http://localhost:8080/)


## Deploying the app

~~~
./gradlew war
~~~

The resulting archive will be in:
`war/build/libs`